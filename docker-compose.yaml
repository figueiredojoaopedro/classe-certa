services:
  postgres:
    image: postgres:18-alpine
    container_name: classe-certa-db
    restart: always
    env_file: "./.env"
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
